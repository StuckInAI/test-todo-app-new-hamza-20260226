version: '3.8'

services:
  web:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DATABASE_PATH=/app/database.sqlite
      - NODE_ENV=production
    volumes:
      - ./database.sqlite:/app/database.sqlite
    restart: unless-stopped